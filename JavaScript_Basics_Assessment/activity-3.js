const studentlist = ['Justin, Maria, Conor'];
for (let i=0; i<3; i++){
    const name= prompt ('add name');
    studentlist.push(name);
}
for (let i=0; i < studentlist.length, i++){
    console.log(studentlist);
}